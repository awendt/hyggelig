<div class="twocol">

<div id="get-started">

<div id="welcome">
hyggelig.org brings <strong>simple event planning</strong> to you and your friends.
It works <strong>without registration</strong> or login,
and anything you submit expires automatically.<br />
<%= link_to 'Learn more', static_path(:tour) %>
</div>

<ol id="instructions">
  <li id="step1"><strong><%= t(:'instructions.create') %></strong><br/><span><%= t(:'instructions.fill_in') %></span></li>
  <li id="step2"><strong><%= t(:'instructions.send') %></strong><br/><span><%= t(:'instructions.mail_url') %></span></li>
  <li id="step3"><strong><%= t(:'instructions.follow') %></strong><br/><span><%= t(:'instructions.follow_replies') %></span></li>
</ol>

</div>

<div class="container" id="create">
<h1><%= I18n.t(:add_event) %></h1>

<%= form_for(@event) do |f| %>

<%= error_messages_header_for 'event' %>

<dl>
<dt><%= label_with_hint :event, :name, t(:'event.name_label'), t(:'event.name_label_hint') %></dt>
<dd><%= f.text_field :name %><%= error_message_on :event, :name %></dd>

<dt><%= label_with_hint :event, :date, t(:'event.time_label'), t(:'event.time_label_hint',
  :other_service => link_to("Snapdecide", "http://www.snapdecide.com")) %></dt>
<dd><%= f.text_field :date %><%= error_message_on :event, :date %></dd>

<dt><%= label_with_hint :event, :location, t(:'event.location_label'), t(:'event.location_label_hint') %></dt>
<dd><%= f.text_field :location %><%= error_message_on :event, :location %></dd>

<dt class="note"><%= t(:'event.will_expire', :date => content_tag(:strong, "#{I18n.l(Event::TIME_TO_LIVE.from_now.to_date, :format => :expiration)}")) %></dt>

<dt><%= submit_tag t(:'event.submit_label') %></dt>
</dl>

<% end -%>

</div>

</div>
